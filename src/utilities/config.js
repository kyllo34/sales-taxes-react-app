// Lookup map for product tax bracket including import
const taxBracket = {
  books: 0,
  goods: 10,
  food: 0,
  medical: 0,
};

const importTax = 5;

module.exports = { taxBracket, importTax };
